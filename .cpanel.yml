---
deployment:
  tasks:
    # IMPORTANT: Keep /home/poti/public_html as target deployment directory
    - export DEPLOYPATH=/home/poti/public_html
    - /bin/rsync -a --exclude='.git' --exclude='.github' ./ $DEPLOYPATH
